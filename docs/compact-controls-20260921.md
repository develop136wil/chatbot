# 결과 카드 조작부 정리 (2026-09-21)

- 더 보기: 13px, 최소 높이 32px, 패딩 5px 12px, 연한 배경/테두리.
  언어별 긴 문구는 고정 폭으로 자르지 않으며 기존 중복 클릭 방어 유지.
- 최신 답변 이동: 중앙 문구 버튼을 36px 원형 아래 화살표로 변경.
  기존 60px 오른쪽 예약 공간 안에 배치한다.
  aria-label과 title은 KO/EN/VI/ZH 선택 언어로 제공한다.
  높이 480px 이하에서는 국기와 겹치지 않도록 숨긴다. 일반 스크롤은 유지.
- 운영 목적/기본검사(무료) 및 명시한 번역 레이블만 소제목으로 표시한다.
  콜론으로 끝나는 독립 레이블이고 뒤에 항목이 있을 때만 적용한다.
  원문·금액·조건·카드 순서·캐시를 수정하지 않는다.
  브라우저 표시 보정이므로 기존 캐시 답변에도 적용된다.
- 검색, AI, 서버 API, SQL, 환경변수 변경 없음.

로컬 검증: Python 115개, JavaScript 95개.
실제 Redis 통합 6개 및 배포 성공은 CI에서 별도 확인.
화면 자동화 도구의 초기화 오류가 있었으므로 실제 모바일 시각 확인과 구분한다.
배포 전 운영 커밋: f4c42de9b3dc836caefaaf37bc092ba4ba7bc553.
